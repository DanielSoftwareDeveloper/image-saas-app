interface SiteProps {
  title: string;
  description: string;
  panelPath: string;
}

export const siteConfig: SiteProps = {
  title: "Templates App",
  description: "My template store",
  panelPath: "/dashboard",
};
