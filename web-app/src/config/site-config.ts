interface SiteProps {
  title: string;
  description: string;
  panelPath: string;
}

export const siteConfig: SiteProps = {
  title: "Genx-AI",
  description: "Generate images with AI with the best AI models",
  panelPath: "/dashboard",
};
